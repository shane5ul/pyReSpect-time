# pyReSpect-time
A rewrite of the python classic library for extracting continuous and discrete relaxation spectra from stress relaxation data.The core algorithms are the same, the interface (both developer and user) is modernized.
